const config = {
    host: "localhost",
    port: 3306, // 3306 for windows/ 8889 linux for newer macs
    uname: "root",
    pword: "", // blank for windows
    dbase: "db_interactive"
}

module.exports = config;